import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  item: {
    width: 100,
    height: 200,
  },
  imageContainer: {
    flex: 1,
    backgroundColor: '#fff',
    borderRadius: 8,
  },
});

export default styles;
